# Browser Agent

AI агент для автоматизации браузера через LLM от Polza.ai с Security Layer.

## Что это такое

AI агент, который принимает текстовые задачи от пользователя и выполняет их в видимом браузере автономно.

**Пример:**
```
python main.py "зайди на яндекс еду, найди пиццу пепперони и добавь в корзину"
```

## Технологии

### browser-use
Основная библиотека для автоматизации браузера через LLM. Отвечает за:
- Управление браузером (Playwright)
- Получение скриншотов и DOM-снимков страницы
- Интерпретацию действий LLM в команды браузера
- Интеграцию с vision-моделями

**Репозиторий:** [browser-use/browser-use](https://github.com/browser-use/browser-use)

## Архитектура

### Security Layer ✅
Интегрированный продвинутый паттерн для защиты от опасных действий.

**Что блокирует:**
- Финальную оплату (checkout, confirm payment, complete purchase)
- Удаление аккаунта (delete account, remove account)
- Открытие новых вкладок (switch_tab, open in new tab)

**Что НЕ блокирует (безопасные контексты):**
- Выбор опций в модальных окнах (select size, choose sauce, add to cart)
- Навигация и заполнение форм

**Умная фильтрация:**
- Анализирует контекст действия
- Различает "checkout" (опасно) и "add to cart" (безопасно)

### Modal Enhancer 🔔
Улучшенная обработка модальных окон с детекцией и приоритизацией.

**Что делает:**
1. **Детектирует** модальные окна по DOM признакам:
   - `role="dialog"` или `role="alertdialog"`
   - `aria-modal="true"`
   - CSS классы: modal, dialog, popup, overlay, lightbox, drawer
2. **Логирует** информацию о модальных окнах для отладки
3. **Даёт рекомендации** агенту для работы с модальными окнами

**Как агент работает с модальными окнами:**
1. **Распознаёт** по визуальным признакам (скриншот + DOM)
2. **Приоритизирует** элементы модального окна над элементами страницы под ним
3. **Выбирает опции** если пользователь не указал — берёт первую или recommended/popular
4. **Добавляет wait** после каждого действия в модальном окне (1-2 секунды)
5. **Проверяет** что появилось после закрытия модального окна

### Усиленный системный промпт для модальных окон 📝

**Новые инструкции для LLM:**
- ПЕРВЫЙ ШАГ перед любым действием — проверить наличие модального окна
- Работать ТОЛЬКО с элементами внутри модального окна
- Игнорировать элементы под модальным окном
- После КАЖДОГО клика в модальном окне — добавить wait 1-2 секунды
- Приоритет кнопок: действие → выбор опции → закрытие

## Установка

## Установка

```bash
cd browseragent

# Создай виртуальное окружение (опционально)
python -m venv .venv
.venv\Scripts\activate  # Windows

# Установи зависимости
pip install -r requirements.txt
python -m playwright install chromium
```

## Конфигурация

Создай файл `.env`:
```
POLZA_API_KEY=твой_ключ_от_polza_ai
```

## Использование

### Базовый пример

```bash
python main.py "зайди на яндекс еду и покажи мне пиццы"
```

### С опциями

```bash
# Видимый браузер (по умолчанию)
python main.py "найди статью про Python на Википедии"

# Фоновый режим
python main.py --headless "открой GitHub и найди browser-use"

# Другая модель
python main.py --model anthropic/claude-sonnet-4 "закажи такси"
```

### Примеры задач

```bash
# Поиск информации
python main.py "найди курс доллара на Центробанке"

# Покупки (с остановкой перед оплатой)
python main.py "зайди на wildberries и найди кроссовки Nike"

# Соцсети
python main.py "открой YouTube и найди видео про browser automation"
```

## Доступные модели (Polza.ai)

- `openai/gpt-4o` — по умолчанию, поддерживает vision
- `openai/gpt-4o-mini` — дешевле и быстрее
- `anthropic/claude-sonnet-4` — Claude Sonnet 4

## Как работает Security Layer

```python
# Перед каждым шагом агента вызывается callback:
async def __call__(self, browser_state, agent_output, step):
    for action in agent_output.action:
        if "checkout" in str(action).lower():
            raise SecurityLayerBlockedAction("Действие заблокировано!")
```

При блокировке:
```
🔒 SECURITY LAYER: Обнаружено опасное действие
============================================================
Действие: click(...)
URL: https://example.com/checkout
============================================================
```

## Ограничения

- Агент сам решает, что делать (нет жёстких инструкций)
- Не оплачивает автоматически без явного указания
- Останавливается после выполнения задачи

## Структура проекта

```
browseragent/
├── main.py              # Точка входа, создание Agent
├── security_layer.py    # Security Layer (интегрирован)
├── modal_enhancer.py    # Modal Enhancer (детекция модальных окон)
├── requirements.txt     # Зависимости
├── README.md            # Документация
└── requements.md        # Требования к решению
```

## Troubleshooting

### Ошибка API ключа
```
❌ Ошибка: POLZA_API_KEY или OPENAI_API_KEY не найден в .env
```
→ Проверь что файл `.env` создан и ключ указан правильно

### Браузер не открывается
→ Установи Chromium:
```bash
python -m playwright install chromium
```
